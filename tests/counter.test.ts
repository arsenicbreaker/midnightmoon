/**
 * Counter contract tests.
 *
 * Fill in after compiling contracts/counter.compact into managed/.
 * Run: npm test
 */

import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("counter", () => {
  it("placeholder — add compile + circuit tests", () => {
    assert.equal(true, true);
  });
});
